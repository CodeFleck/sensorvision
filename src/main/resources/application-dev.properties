# Development Environment Configuration
spring.profiles.active=dev

# Feature Flags - Disabled by default in DEV
sms.enabled=false
email.enabled=false

# Override these with environment variables when ready:
# SMS_ENABLED=true
# EMAIL_ENABLED=true

# Logging
logging.level.org.sensorvision.service.notification=DEBUG
